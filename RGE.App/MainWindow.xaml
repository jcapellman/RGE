<Window x:Class="RGE.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RGE.App"
        xmlns:viewModels="clr-namespace:RGE.App.ViewModels"
        mc:Ignorable="d"
        Title="RGE" Height="600" MinWidth="320" MinHeight="200" Width="800">
    <Window.DataContext>
        <viewModels:MainWindowVM />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="20" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="Red">
            <TextBlock Foreground="White" FontWeight="Bold" TextAlignment="Center" Text="{Binding Game.TitleBarStr}" />
        </Grid>

        <Grid Grid.Row="1" Background="Black">
            
        </Grid>
    </Grid>
</Window>
